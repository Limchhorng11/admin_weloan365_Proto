# Public app metadata (exposed to the browser)
NEXT_PUBLIC_APP_NAME=WeLoan365
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only secrets (DO NOT prefix with NEXT_PUBLIC_)
# DATABASE_URL=
# AUTH_SECRET=
